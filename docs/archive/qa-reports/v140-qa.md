# v1.4.0 fix batch — QA エビデンス（2026-06-10）

## TDD 来歴

- T0〜T16 の各タスクで RED→GREEN→ミラー同期→コミットを完走（計画 docs/plans/2026-06-10-v140-fix-batch-implementation-plan.md）
- テスト件数: 332（v1.3.3）→ 383（T16 時点）→ **389**（grill 🟡 修正後）
- grill 🟡-1: ミラー摂動（example hook へ 1 行追記）で RED=FAILED (failures=1) を実証→復元で GREEN=OK
- grill 🟡-2: 形式テスト 2 件が template 16 + example 16 コマンドの旧形式で RED→フォールバック形へ統一で GREEN。contract validator のヘルパーテスト 3 件も RED（AttributeError）→実装で GREEN
- 途中検出: contract が example settings の新形式参照 16 件を解決不能で FAIL → defense in depth が機能。`script_rel_from_command` で両形対応し PASS 復帰

## 検証コマンドと結果（最終、c3e007a 時点）

| 検証 | コマンド | 結果 |
|------|---------|------|
| 全ユニットテスト | `python3 -m unittest discover -s tests` | **389 tests OK**（v1.3.3 比 +57） |
| contract | `python3 scripts/check_framework_contract.py` | PASS: aegis contract is aligned |
| 参照 drift / mirror | `python3 scripts/check_reference_drift.py` | PASS: no reference drift detected |
| STATUS 厳格検証 | `python3 scripts/check_status.py --strict` | PASS |
| tier2 scaffold smoke | `python3 scripts/eval_scaffold_smoke.py` | PASS（minimal/standard/full） |
| 旧形式残存 | `grep -rn 'grep -A20' hooks/ scripts/ examples/` | 0 件 |

## 実地検証（抜粋）

- CLAUDE_PROJECT_DIR 未設定環境で `bash "${CLAUDE_PROJECT_DIR:-.}"/hooks/pre-compact.sh` を実発火 → 正常な block 判定 JSON が返る（hook 生存＝fail-open しない）
- update-gate ロック: lock 保持中の並行実行が rc≠0＋STATUS 無変更、解放後は正常承認＋lock dir 解放（test_update_gate_lock.py 3 件）
- failure policy 表: 16 hook 全行を python3 遮断環境で実発火し宣言と突合（test_failure_policy.py）
- B1 ドリル恒久修正: tracked docs/ 変更が coverage floor から除外され、src のみの mutant で PASS する E2E を確認
