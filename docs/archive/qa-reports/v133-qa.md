# v1.3.3 P1 fix-forward — QA エビデンス（2026-06-10）

## TDD 来歴

1. **RED（第1ラウンド）**: 実運用 hook 入力エンベロープ（session_id/transcript_path/cwd/hook_event_name/tool_name 込み）で 22 テスト追加 → 9 FAIL を確認（P1-1: git status/npm test/pytest/src-hooks コマンドが deny、P1-2: src/hooks/・ネスト CLAUDE.md・src/templates/ 等が deny）
2. **GREEN（第1ラウンド)**: hook 修正後 22/22 PASS
3. **RED（第2ラウンド・grill 指摘）**: バイパス形 12 テスト追加 → 11 FAIL を確認（`../hooks/`・`foo/../hooks/`・絶対パス・物理パス・`$(pwd)/`・`$VAR/`・`././hooks/` 等が allow に反転していた）
4. **GREEN（第2ラウンド)**: 強化後 34/34 PASS

## 検証コマンドと結果（最終）

| 検証 | コマンド | 結果 |
|------|---------|------|
| 全ユニットテスト | `python3 -m unittest discover -s tests` | **332 tests OK**（v1.3.2 比 +34） |
| contract（framework） | `check_framework_contract.py --profile=full` | PASS |
| contract（scaffold） | `check_framework_contract.py --profile=standard` | PASS |
| 参照 drift / mirror | `check_reference_drift.py` | PASS |
| tier2 scaffold smoke | `eval_scaffold_smoke.py` | PASS（minimal/standard/full） |

## 実地再現（修正前後の実発火比較）

/tmp フィクスチャ（task_type=feature の STATUS.md＋hook＋lib）に対し、実運用エンベロープ（transcript_path=~/.claude/projects/...）で発火:

- 旧 hook（`git show HEAD:hooks/check-control-plane.sh`）＋ `git status` → **deny**（P1-1 再現）
- 新 hook＋同一入力 → **allow（`{}`）**
- 新 hook＋`sed -i 's/a/b/' hooks/check-gate.sh` → **deny 維持**（fail-open なし）

## 回帰シール（再発封鎖）

- `eval_scaffold_smoke.py` の検証 stdin を `_hook_stdin()`（実運用スキーマ）へ全面移行。**検査入力の現実度を契約化**（F6 族の死角封鎖）
- B-4 シール: install 先で `git status`→allow かつ hooks/ 書込→deny を 3 profile で実発火検証
- B-5 シール: `src/hooks/` が `[integrity]` deny に分類されないことを実発火検証
- 旧 hook では B-4/B-5 とも確実に FAIL することを grill レビュアーが独立実証
