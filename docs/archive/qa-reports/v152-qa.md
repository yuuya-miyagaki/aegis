# v1.5.2 残余全消化バッチ — QA エビデンス（2026-06-11）

## TDD 来歴

- Task 1〜9＋grill-code 修正 1 件を RED→GREEN→ミラー同期→コミットで完走（計画 docs/plans/2026-06-11-v152-residual-elimination-implementation-plan.md）
- テスト件数: 461（v1.5.1）→ 478（Task 1〜7）→ **479**（grill J1 修正）
- 代表的な RED 実証（いずれも計画記載の期待 RED と一致）:
  - Task 1（T1a）: STRIP パターン＋parity ハーネス拡張で 2 failures + 2 errors RED → patterns.sh の `AEGIS_TR_STRIP_DQ/SQ` 定義＋`normalize_sed`/`normalize_py`（実 sed/tr を実行）で GREEN。`('"echo" pytest', False)` の反転 fixture で「置換であって削除でない」を固定
  - Task 2（T1b）: 消費者 2 系統（post-bash.sh / build-judge-card.py）の配線で 3 failures RED → `tr '\n' ';' | sed -E s/…/Q/g` ×2 と `_tr_strip_patterns`＋`sp.sub("Q", cmd)` で GREEN。`len(strips) != 2 → unverified` の fail-closed ガード同梱
  - Task 3（T2）: `(( pytest ))`・`( (vitest run))` fixtures RED → アンカー `\(? *` を `(\( *)*` へ拡張で GREEN。`grep "(pytest x`（不正クォート）は不一致を維持
  - Task 4（T3）: raw JSON `"ls tests\\/unit"` の fidelity テスト RED → ルーティングクラスへ `/` 追加（`'\\[\\nrtbfu"/]'`）で GREEN
  - Task 5（T4a）: 孤児 claim 復元 2 件 RED → dead claimer かつ pid 不在時のみ `mv claim → pid` で GREEN（live claimer・非数値は不介入を同時固定）
  - Task 6（T4b）: pid なしロック採用 4 件 RED → `find -maxdepth 0 -mmin +1` 年齢ゲート＋`( set -C; printf > pid )` O_EXCL で GREEN（空/garbage pid は構造的に採用不能＝fail-closed 維持）
  - Task 7（T5）: 待機窓・競合テスト RED → `{1..50}`×0.2s=10s で GREEN。light ゲート競合は両者成功（意図的仕様変更）
  - grill A 🟡 J1: `sp.sub("Q")`→`sp.sub("")` 削除変異下で新テスト RED（FAILED 1）→ 変異 revert で GREEN を両方向実走証明

## 検証コマンドと結果（最終、grill 修正後 = b79184a 時点）

| 検証 | コマンド | 結果 |
|------|---------|------|
| 全ユニットテスト | `python3 -m unittest discover -s tests` | **479 tests OK**（v1.5.1 比 +18） |
| contract（本体） | `python3 scripts/check_framework_contract.py` | PASS: aegis contract is aligned |
| contract（example） | `--profile standard --root examples/minimal-project` | PASS: project contract is aligned |
| 参照 drift / mirror | `python3 scripts/check_reference_drift.py` | PASS: no reference drift detected |
| tier2 scaffold smoke | `python3 scripts/eval_scaffold_smoke.py` | PASS（minimal/standard/full） |
| STATUS 厳格検証 | `python3 scripts/check_status.py --strict` | PASS |

## 実地検証（抜粋）

- レースドリル（Task 9）: 3 contenders（brainstorm approve / security reset / client_ready_for_dev reset）× 15 回で rcs 全クリーン・torn write ゼロ・ロック残留ゼロ・gate 値整合（brainstorm approved + security pending）
- マスクのエンジンパリティ: `test_mask_engines_agree` が sed -E と python re のマスク出力バイト一致を fixture 全件で固定。grill B が独立 7 ケースでも一致を実証
- 分類正味挙動: `grep -E "(unittest|pytest)" missing.txt`（fail）が直前の実 green を覆さない e2e（`test_quoted_runner_mention_failure_does_not_red`）、`'"echo" pytest'`（ok）が green 化しない mutation-killer（`test_mask_is_substitution_not_deletion`）
- deny 系不波及: `TestMaskScopeBoundary` が check-destructive / check-control-plane / check-secrets の 3 hook に `AEGIS_TR_STRIP` 参照ゼロを契約化。grill B が `git reset --hard HEAD` の従来 ask 維持を実走確認
- テスト強度: grill B が T1a/T1b/T2/T4a/T4b を一時 revert → 各該当テストが RED に倒れることを実証（自己複製なし）
- ミラー同一性: 変更 5 ファイル（patterns.sh / extract-input.sh / post-bash.sh / build-judge-card.py / update-gate.sh）全て diff で byte 同一（grill 両者＋本セッションで実測）
