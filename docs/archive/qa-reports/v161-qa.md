# v1.6.1 QA エビデンス（2026-06-13）

## 検証結果（全 PASS）

| 検査 | 結果 |
|------|------|
| `python3 -m unittest discover tests -p "test_*.py"` | **606 tests PASS**（既存 508 → 新規 98 件追加、skipped=1 shellcheck 未インストール） |
| `python3 scripts/check_framework_contract.py` | PASS（本体） |
| `python3 scripts/check_reference_drift.py` | PASS（12 チェック全 PASS、本体↔example の mirror byte-identical 確認） |
| `python3 scripts/check_status.py --strict` | PASS |
| `python3 scripts/eval_scaffold_smoke.py` | PASS（minimal / standard / full 全 3 プロファイル） |

## 新規テストファイル一覧（v1.6.1）

| ファイル | テスト数 | 対象 |
|---------|--------|------|
| `tests/test_secrets_pattern_single_owner.py` | 9（1 skipped） | Task 0 / C-9 |
| `tests/test_secrets_pattern_consumer.py` | 9 | Task 6 / C-9 |
| `tests/test_secrets_git_variants.py` | 17 | Task 7 / S-3 + grill A-Crit-4 / A-S4 |
| `tests/test_control_plane_var_expansion.py` | 22 | Task 1 / C-1 + grill A-Crit-1 |
| `tests/test_test_runner_realness.py` | 23 | Task 2 / C-2 + grill A-Crit-2 / B-Critical |
| `tests/test_client_ready_artifact_content.py` | 5 | Task 3 / C-3 |
| `tests/test_session_start_matcher.py` | 3 | Task 4 / C-4 |
| `tests/test_arch_overview_currency.py` | 5 | Task 5 / C-5 + C-6 |
| `tests/test_phase_skills_required.py` | 5 | Task 8 / S-11 + grill B-S2 |
| **合計** | **98** | — |

## 既存テスト変更

- `tests/test_check_status.py`: CLIENT_GATE_ARTIFACTS の tuple 化に伴う fixture 更新（5 fixtures）。1 件の error-message format 更新
- `tests/test_judge_card.py`: synthetic observed entry で `marker_verified:True` をデフォルト化（schema migration 対応）
- `tests/test_evidence_hooks.py`: `bash_payload` に `output` 引数追加、現実的 unittest summary を fixture 化

## 実行時間プロファイル

| ファイル | 時間 | 備考 |
|---------|------|------|
| `test_update_gate_lock.py` | ~89s | プロダクション側の lock wait loop に起因（v1.7 で `--wait-iterations` 差し込み口を追加予定） |
| `test_phase_skill_injection` 系 | ~70s 計 | scaffold smoke を tier 別に再走 |
| 残り全テスト | ~10s | |
| **合計** | ~139s | v1.6.0 比 ~7s 増（98 件追加分） |

## 既知の挙動（v1.6.1）

1. **observer entry の v1.6.0 → v1.6.1 マイグレーション**: `marker_verified` 不在のログエントリは強制 `unverified` 化（fail-closed schema migration）。再走で上書きされるまでは旧 entry は green と扱われない
2. **cmd_var_built_write の ask クラス**: 変数代入＋変数参照＋write op の 3 条件 AND を満たす正当 build script は ask に倒れる（mild friction）
3. **template の sentinel コメント**: ユーザがテンプレ末尾の `<!-- aegis-required-section: X -->` を削除すると client_ready_for_dev gate が deny
