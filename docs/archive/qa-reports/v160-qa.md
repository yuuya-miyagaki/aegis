# v1.6.0 fix-forward P1×4 バッチ — QA エビデンス（2026-06-12）

## TDD 来歴

- Task 1〜15＋grill-code 修正 1 件（S1）を RED→GREEN→ミラー同期→コミットで完走（計画 docs/plans/2026-06-12-fix-forward-p1-implementation-plan.md、各 Task の期待 RED は計画に明記）
- テスト件数: 479（v1.5.2）→ 507（Task 1〜15）→ **508**（grill S1 修正）
- 代表的な RED 実証（本セッションで直接観測した分）:
  - Task 12: `test_legit_transition_injects_new_phase_skills` が `'' に .claude/skills/subagent-dev/SKILL.md なし` で FAIL（現行 `{}` 返却）→ post-status-audit.sh の snapshot 更新後ブロックに `aegis_phase_skill_paths` 由来の `emit_context PostToolUse` を追加して GREEN。`test_no_transition_emits_allow` は遷移なし＝素の `{}` を同時固定
  - Task 13: `check_skill_reachability` 未定義の AttributeError ×6 RED → BFS 実装（phase map / user-invocable / 制御ファイル path 参照の 3 root 種＋skill→skill edge 伝播）で 6 PASS。**計画乖離 1 件**: 実 repo RED 観測が期待（browser-assist/integration-assist ×2）に反し CLEAN → 原因は `check_framework_contract.py` の存在マニフェスト（全 18 skill の path リテラル）が root 扱いされる false-negative ＝検査の空洞化。`SKILL_REF_EXCLUDE` で除外し `test_existence_manifest_does_not_rescue`（RED→GREEN）でピン → 期待どおりの 2 件 RED に復元
  - Task 14: 正規化 9 ファイル反映後、同コマンドで CLEAN（GREEN 観測）。`check_session_start_hints` 削除＋ALL_CHECKS swap
  - Task 15: 壊した install（full scaffold から integration-specialist.md を削除）で `integration-assist has no boot path` 1 件を検出（検出力実証）。tier2 全 profile PASS
  - grill S1: `test_comment_names_example_is_not_root` が旧 regex で RED（コメント中の `names="phantom"` 例文が root 化され failures=0）→ `^[^#\n]*\bnames="([^"\n]*)"` (re.M) で GREEN。修正後の実 repo トークン集合＝実 skill 名 15 件ちょうどを実測
- Task 1〜11 の RED→GREEN はコミット列（16c5db9〜2e093e6）と計画の Step 構成で記録（vendor 除外 / decode 耐性 ×2 / card push / gate 順序 / client 検査 ×2 / templates 配布 / smoke 参照検証 / phase-skills lib / session-start 注入）
- 付随修正: Task 11/12 が hook に `lib/phase-skills.sh` 依存を追加したため、lib を部分 staging する 2 fixture（test_check_status の TempProjectWithHooks / test_evidence_lib の TestSessionStartRotates）に symlink/copy を追加（c6e4937、7 件の rc=1 を解消）

## 検証コマンドと結果（最終、grill 修正後 = a8411fb 時点）

| 検証 | コマンド | 結果 |
|------|---------|------|
| 全ユニットテスト | `python3 -m unittest discover tests/` | **508 tests OK**（v1.5.2 比 +29） |
| contract（本体） | `python3 scripts/check_framework_contract.py` | PASS: aegis contract is aligned |
| contract（example） | `--profile standard --root examples/minimal-project` | PASS: project contract is aligned |
| 参照 drift / mirror / 到達性 | `python3 scripts/check_reference_drift.py` | PASS: no reference drift detected（skill reachability 含む） |
| tier 0 | `python3 scripts/run_eval.py --tier 0` | PASS（timeout 120→300s へ追従。スイート実測 ~130s） |
| tier 1 | `python3 scripts/run_eval.py --tier 1` | PASS (with warnings) |
| tier 2 scaffold smoke | `python3 scripts/run_eval.py --tier 2` | PASS（minimal/standard/full — install 先 reachability＋template 参照検証込み） |
| tier 3 | `python3 scripts/run_eval.py --tier 3` | PASS |
| STATUS 厳格検証 | `python3 scripts/check_status.py --root . --strict` | PASS |

## 実地検証（抜粋）

- install 先到達性の検出力: full scaffold の `agents/integration-specialist.md` 削除（integration-assist の唯一 root 破壊）で `no boot path` 1 件を検出。無傷 install は 3 profile とも CLEAN
- phase 遷移注入: temp repo（hooks 実体 copy・snapshot brainstorm→STATUS plan）への post-status-audit.sh 実走で `additionalContext` に `.claude/skills/subagent-dev/SKILL.md` を確認。遷移なしでは `{}`
- 到達性 regex の境界: コメント行 `# parses the \`names="phantom"\` ...` が root 化されないこと、`implement) names="tdd" ;;`（単行 case 形）が root 化されることを同一テストで両面固定
- grill A（独立）: /tmp scaffold 3 profile で reachability CLEAN・templates 6 件配布を実測、deny 系 4 種（gate-tamper / phase-skip / mode-tamper / placeholder contract）の hook 実走無傷を確認
- grill B（独立）: PHASE_MAP_NAMES_RE の偽トークン混入を実機実証（S1 の根拠）、update-gate.sh の rc=0/2/1×ack 経路追跡、mirror byte-identical 実測
- ミラー同一性: `test_mirror_identity` 7 PASS（Task 12/14 の同期後に実測）
