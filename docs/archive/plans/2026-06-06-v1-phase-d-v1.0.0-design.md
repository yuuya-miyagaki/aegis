# Phase D（仕上げ）v1.0.0 設計書

> 再アーキ F→R→A→D の最終フェーズ。F/R/A は完了済み（`2026-06-05-v1-future-proof-rearchitecture-design.md` §7 状態参照）。Phase D で「トレッドミルから降りる」看板を v1.0.0 として掲げる。

**版:** v1.0.0 想定 / **日付:** 2026-06-06 / **mode:** Dev / **task_type:** framework

---

## 1. 目的とスコープ

哲学変更フェーズ（model/effort・TDD profile・evidence 完了強制）が揃い、§9 の「v1.0.0 は哲学フェーズを通し終えた後」の条件が満たされた。Phase D は**最小セット**で 1.0.0 を仕上げる。読者はソロ＋uccc 中心。

**in:**
1. migration guide（v0.12.2 → v1.0.0）
2. README リフレッシュ（stale 修正）
3. 安定契約＋SemVer ポリシー明文化
4. version 1.0.0 bump ＋ git tag

**out（YAGNI）:** INTEGRATION フル文書／LEARNINGS 大規模整備／CHANGELOG 新設／外部公開向けの厚いガイド。理由は manifest を descope したのと同じ＝実消費者が uccc 中心の現状で過剰。

## 2. migration guide（v0.12.2 → v1.0.0）

唯一の git tag `v0.12.2` を起点に、それ以降の全変更を **README の既存 `## Migration` ログにインライン追記**（`### From v0.12.2 to v1.0.0`）。既存が時系列インライン形式のため一貫。

> 注: README 既存ログ末尾の stale 記述（`From v0.12.1 to v0.12.2` エントリ末の「The remainder of v0.13.0 ... follows」）も §3 で修正する。新エントリは Migration 節の先頭（最新が上）か末尾か、既存の並び順に合わせる（着手前に確認）。

カバーする破壊/注目変更:
- **skill `aegis-*` 改名**（最大の破壊・外部参照を壊す）→ 旧名→新名の対応表（brainstorm→aegis-brainstorm 等）
- 新 hook 群（Skill/Cron gate・TaskCreated=`continue:false` hard stop／TaskCompleted=exit2 差し戻し）の追加挙動
- **evidence 完了強制**（TaskCompleted で gate-ref 不整合・ref 不在を exit2 差し戻し・v0.12.6）
- model/effort ポリシー（agent frontmatter が pin/inherit・haiku 全廃）
- routing.md 縮約（specialist 起動の委譲）・CLAUDE.md context 数値撤廃・TDD `AEGIS_TDD_MODE=off` escape hatch
- emit.sh/patterns.sh 単一出力源（挙動不変・内部リファクタ）
- **Action for existing projects:** `bash bin/setup.sh --profile=<p>` 再実行＋skill 参照の改名対応（特に uccc）

代替案（不採用）: standalone `docs/MIGRATION-v0.12-to-v1.md`。→ 既存ログとの一貫性とファイル数最小を優先しインライン採用。

## 3. README リフレッシュ

- stale 記述の修正: 「v0.13.0 follows」系（v0.13.0 は 0.12.x 再アーキ線に再編された事実へ）。
- version 言及・profile 説明・skill 名の古い箇所を実態へ（ピンポイント）。
- 348行の構成は維持・大改訂しない。

## 4. 安定契約＋SemVer ポリシー

README に短い節を新設（`## Stability & Versioning`）:
- **公開契約（壊すなら major）**: `CLAUDE.md` 操作ルール／`STATUS.md` スキーマ／profile 名（minimal/standard/full）／gate モデル／hook 出力スキーマ（emit.sh 経由）。
- **内部（自由に変更）**: スクリプト内部・正確な文言・agent プロンプト・テスト・行数・コメント言語等。
- SemVer: 公開契約破壊=major／後方互換な追加=minor／修正=patch。
- 配置は README（対外説明）。CLAUDE.md は word budget があり運用契約本体なので置かない。

## 5. version 1.0.0 ＋ tag

- `scripts/check_framework_contract.py` `FRAMEWORK_VERSION` `0.12.6 → 1.0.0`
- `templates/STATUS.template.md` `framework_version: 1.0.0`
- `docs/STATUS.md` `framework_version: 1.0.0`
- 実装・push 完了後に `git tag v1.0.0`（tag 付与は push 同様ユーザー確認の上）

## 6. 検証

- `check_framework_contract.py`（version sync）／`check_reference_drift.py`／全テスト緑／tier 1/2 eval PASS。
- README の migration が参照するファイル/skill 名の実在チェック（リンク切れ・旧 skill 名の取り違え防止）。
- migration の旧→新 skill 対応表が実際の `.claude/skills/` と一致するか grep 突合。

## 7. footgun / 注意

- README の word 数・構成は維持（大改訂しない）。
- skill 改名対応表は**実機の `.claude/skills/` を grep して正**とする（記憶で書かない）。
- version は contract が STATUS.template と sync を FAIL 強制 → bump は対で。
- tag `v1.0.0` は push 後。tag も push もユーザー確認。

## 8. 非スコープ

- INTEGRATION フル文書・CHANGELOG・LEARNINGS 大規模整備。
- manifest（descoped 継続）・context observability（YAGNI 保留継続）。
- 公開契約に skill 名・CLI フラグ・ファイルレイアウトを含めない（決定 a: 運用契約のみ安定面）。
