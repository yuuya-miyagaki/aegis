# Phase D（v1.0.0 仕上げ）Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 最小セットで aegis を v1.0.0 に仕上げる — migration guide（v0.12.2→v1.0.0）・README stale 修正・安定契約/SemVer 明文化・version 1.0.0 bump＋tag。

**Architecture:** ほぼドキュメント編集＋version bump。新規コードなし＝TDD ではなく検証ベース（contract/drift/全テスト緑維持＋tier 1/2 eval＋migration の skill 名を実機 grep で突合）。README は既存 348 行構成を維持しピンポイント編集。

**Tech Stack:** Markdown、Python（contract/eval）、git tag。

**設計書:** `docs/plans/2026-06-06-v1-phase-d-v1.0.0-design.md`

---

## ベースライン（着手前に確認）

`aegis/` で実行:
- `python3 -m unittest discover -s tests -q 2>&1 | tail -2` → `Ran 195 tests ... OK`
- `python3 scripts/check_framework_contract.py` → exit 0 / `python3 scripts/check_reference_drift.py` → exit 0
- `python3 scripts/run_eval.py --tier 1` / `--tier 2` → `Result: PASS`
- 現行 version: `FRAMEWORK_VERSION = "0.12.6"`（`scripts/check_framework_contract.py:17`）/ `framework_version: "0.12.6"`（`templates/STATUS.template.md:3`）
- 唯一の git tag: `v0.12.2`（`git tag --list`）
- README `## Migration` 節は**新しい順**（先頭が最新 `### From v0.12.1 to v0.12.2`・README:166）

**確定事実（実機確認済み・migration table の正）:**
- skill 改名（`d6430c1`・公式同名衝突回避）: `brainstorming → aegis-brainstorm` / `review → aegis-review-gate` / `security-review → aegis-security-gate`
- 現在の `.claude/skills/`: aegis-brainstorm, aegis-review-gate, aegis-security-gate, browser-assist, bug-diagnosis, client-workflow, deploy, docs-sync, integration-assist, qa-verification, session-recovery, ship-and-docs, subagent-dev, tdd, translation-mapping

> **⚠ footgun**:
> - README は大改訂しない（ピンポイント）。
> - migration の skill 名は**実機 grep を正**とする（Task 4 で突合）。
> - version bump は `check_framework_contract.py` と `STATUS.template.md` を**対で**（contract が sync を FAIL 強制）。`docs/STATUS.md` も併せて。
> - tag `v1.0.0` も push も**ユーザー確認の上**。

---

## Task 1: migration guide（v0.12.2 → v1.0.0）

**Files:**
- Modify: `README.md`（`## Migration` 直後・`### From v0.12.1 to v0.12.2` の**手前**に新エントリを挿入）

- [ ] **Step 1: 新エントリを Migration 節の先頭に挿入**

`README.md` を Edit:
- old:
```
## Migration

### From v0.12.1 to v0.12.2
```
- new:
```
## Migration

### From v0.12.2 to v1.0.0

**Future-proof re-architecture (F→R→A→D).** Everything since the `v0.12.2` tag,
consolidated into the v1.0.0 milestone. The old "v0.13.0" line was reframed onto
the `0.12.x` series and lands here.

**Breaking — skill renames** (Phase 0b, official-name collision avoidance). Update
any external references (e.g. uccc) to the new names:

| Old skill | New skill |
|-----------|-----------|
| `brainstorming` | `aegis-brainstorm` |
| `review` | `aegis-review-gate` |
| `security-review` | `aegis-security-gate` |

**New enforcement / behavior:**

1. **New gate hooks**: `check-skill-gate.sh` (Skill), `check-cron-gate.sh` (CronCreate),
   and Task event hooks — `check-task-created.sh` (TaskCreated → `continue:false` hard
   stop when a gate blocks a new task) and `check-task-completed.sh` (TaskCompleted →
   `exit 2` push-back).
2. **Evidence-completion enforcement** (v0.12.6): TaskCompleted pushes back when an
   approved `review`/`qa`/`security`/`deploy`/`plan` gate has no `current_refs` entry,
   or a declared ref points to a missing file. Same invariant as `check_framework_contract`.
3. **Model/effort policy**: agent frontmatter is now pinned by role tier (quality
   roles on `opus`, cost roles on `sonnet`, default `inherit`); `haiku` removed.
4. **TDD profile**: the `check-tdd.sh` backstop ships only in `full`; within `full`,
   `AEGIS_TDD_MODE=off` disables it for a session (session-start warns).
5. **Leaner rules**: `routing.md` reduced to principles + agent manifest; `CLAUDE.md`
   dropped the hard context-doc count (pull-based).
6. **Internal (behavior-unchanged)**: hook output schemas unified in `hooks/lib/emit.sh`,
   destructive patterns in `hooks/lib/patterns.sh`.

**Action for existing projects**: re-run `bash bin/setup.sh --profile=<your-profile>`
to refresh `.claude/settings.local.json` and hooks, then update any external skill
references to the renamed `aegis-*` skills above.

### From v0.12.1 to v0.12.2
```

> 注（歴史エントリの据置）: README 既存の古い migration エントリ（例 248-303 行）には旧 skill 名 `review`/`security-review`/`brainstorming` が出てくるが、これらは**当時その名前だった歴史記録**（本文 1-163 行には旧名なしを確認済み）。**改竄しない**＝据置が正しい。改名は本新エントリの対応表で明示する（CHANGELOG 哲学）。本文と新エントリのみ現行名を使う。

- [ ] **Step 2: 挿入位置と Markdown を確認**

Run: `grep -nE "^### From v" README.md | head -3`
Expected: `### From v0.12.2 to v1.0.0` が先頭（最小の行番号）、次に `### From v0.12.1 to v0.12.2`。

- [ ] **Step 3: コミット**

```bash
git add README.md
git commit -m "$(cat <<'EOF'
docs(readme): add v0.12.2 -> v1.0.0 migration guide

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 2: README stale 修正 ＋ Stability & Versioning 節

**Files:**
- Modify: `README.md`（line 182 付近の stale 一文・新節 `## Stability & Versioning`）

- [ ] **Step 1: stale な「v0.13.0 follows」一文を修正**

`README.md` の `### From v0.12.1 to v0.12.2` エントリ末尾を Edit:
- old:
```
Driven by a 5-round external review (Round 1〜5, 25 issues raised, all reflected). See `docs/plans/v0130-modernization-plan.md` Rev.5 for full context. The remainder of v0.13.0 (subagent / effort / Plan / Skill / TaskCreated|Completed event hooks etc.) follows.
```
- new:
```
Driven by a 5-round external review (Round 1〜5, 25 issues raised, all reflected). See `docs/plans/v0130-modernization-plan.md` Rev.5 for full context. The follow-on work (Skill/Cron gates, Task event hooks, the re-architecture) shipped on the `0.12.x` line — see *From v0.12.2 to v1.0.0* above.
```

- [ ] **Step 2: `## Stability & Versioning` 節を追加**

`README.md` の `## Language Policy` 節の**直前**に新節を挿入（着手前に `grep -n "^## Language Policy" README.md` で位置確認）:
- old:
```
## Language Policy
```
- new:
```
## Stability & Versioning

As of v1.0.0, Aegis follows semantic versioning against a defined **public contract**.

**Public contract (a breaking change bumps MAJOR):**

- the `CLAUDE.md` operating rules (gates, completion rule, state machine)
- the `docs/STATUS.md` frontmatter schema
- profile names: `minimal`, `standard`, `full`
- the gate model (gate names, gate→evidence-ref coupling)
- hook output schemas (as emitted via `hooks/lib/emit.sh`)

**Internal (may change in any release):** script internals, exact message wording,
agent prompts, tests, file line counts, and comment language.

MAJOR = a public-contract break; MINOR = backward-compatible additions; PATCH = fixes.

## Language Policy
```

- [ ] **Step 3: 他の stale を grep で点検（任意修正）**

Run: `grep -niE "ufcc|0\.13\.0|the remainder of|coming soon|TODO" README.md`
Expected: 残る stale があれば個別修正。`AEGIS_TDD_MODE` を含む正当な行（README:100）は対象外。

- [ ] **Step 4: コミット**

```bash
git add README.md
git commit -m "$(cat <<'EOF'
docs(readme): fix stale v0.13.0 note, add Stability & Versioning policy

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 3: version 1.0.0 bump

**Files:**
- Modify: `scripts/check_framework_contract.py:17`
- Modify: `templates/STATUS.template.md:3`
- Modify: `docs/STATUS.md`（frontmatter `framework_version`）

- [ ] **Step 1: FRAMEWORK_VERSION を 1.0.0 へ** — old `FRAMEWORK_VERSION = "0.12.6"` → new `FRAMEWORK_VERSION = "1.0.0"`
- [ ] **Step 2: STATUS.template.md を 1.0.0 へ** — old `framework_version: "0.12.6"` → new `framework_version: "1.0.0"`
- [ ] **Step 3: docs/STATUS.md を 1.0.0 へ** — old `framework_version: "0.12.6"` → new `framework_version: "1.0.0"`

- [ ] **Step 4: contract / drift / 全テスト / tier eval**

Run: `python3 scripts/check_framework_contract.py; echo "contract=$?"; python3 scripts/check_reference_drift.py; echo "drift=$?"; python3 -m unittest discover -s tests -q 2>&1 | tail -2; python3 scripts/run_eval.py --tier 1 2>&1 | tail -1; python3 scripts/run_eval.py --tier 2 2>&1 | tail -1`
Expected: `contract=0`（version sync）、`drift=0`、`Ran 195 tests ... OK`、tier1/2 とも `Result: PASS`。

- [ ] **Step 5: コミット**

```bash
git add scripts/check_framework_contract.py templates/STATUS.template.md docs/STATUS.md
git commit -m "$(cat <<'EOF'
chore(version): bump framework to 1.0.0

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

---

## Task 4: 検証（migration の正確性・リンク健全性）

**Files:** なし（検証のみ）

- [ ] **Step 1: migration の旧→新 skill 名を実機と突合**

Run: `for s in aegis-brainstorm aegis-review-gate aegis-security-gate; do test -d ".claude/skills/$s" && echo "NEW ok: $s" || echo "NEW MISSING: $s"; done; for s in brainstorming review security-review; do test -d ".claude/skills/$s" && echo "OLD still present (unexpected): $s" || echo "OLD gone ok: $s"; done`
Expected: 新3つ存在・旧3つ不在。README の対応表と一致。

- [ ] **Step 2: README が参照するファイルの実在チェック**

Run: `grep -oE "docs/[A-Za-z0-9._/-]+\.md" README.md | sort -u | while read f; do test -f "$f" && echo "ok $f" || echo "MISSING $f"; done`
Expected: `MISSING` がゼロ（migration/本文が指すファイルが全て実在）。

- [ ] **Step 3: Stability 節が挙げる契約要素の実在チェック**

Run: `test -f hooks/lib/emit.sh && echo "emit.sh ok"; grep -qE "minimal|standard|full" README.md && echo "profiles ok"; grep -q "gate" .claude/rules/state-machine.md && echo "gate model ok"`
Expected: 全て ok（README が約束する契約要素が実体として存在）。

---

## Task 5: bookkeeping ＋ 計画 tracked ＋ memory ＋ push ＋ tag（要ユーザー確認）

- [ ] **Step 1: 再アーキ設計の Phase D を完了表記**

`docs/plans/2026-06-05-v1-future-proof-rearchitecture-design.md`:
- §7 状態行（着手前 `grep -n "状態（2026-06-06）"` 確認）の「残るは D のみ」を「**F/R/A/D 全完了（v1.0.0）**」へ更新。
- §11 の `- [ ] **README/LEARNINGS/INTEGRATION/migration guide、version v1.0.0**（← Phase D・唯一の残作業）` を（`grep -n "Phase D・唯一"` 確認後）`[x]` 化:
  - 変更後: `- [x] README リフレッシュ＋migration guide(v0.12.2→v1.0.0)＋安定契約/SemVer 明文化＋version 1.0.0（2026-06-06・Phase D。INTEGRATION/CHANGELOG/LEARNINGS 大規模は YAGNI で out）`

- [ ] **Step 2: STATUS.md を完了状態へ**（着手前に現値確認）
- `gate_approvals.plan: pending → approved`
- `phase: plan → implement`（または docs。task_size=M なので impl→review→ship）
- `current_refs.plan: null → docs/plans/2026-06-06-v1-phase-d-v1.0.0-implementation.md`
- `next_action`: 「Phase D 実装完了（v1.0.0）。再アーキ F→R→A→D 全完了。次は grill-code → push → git tag v1.0.0」
- `last_updated`: 当日 / session_history は frontmatter 最大3件厳守（最古を落として3件）＋ body に1行
- **evidence 整合**: plan approved にしたら plan ref（本実装計画・実在）を必ずセット（gate-ref 不整合で自 hook に弾かれないため）

- [ ] **Step 3: bookkeeping をコミット**

```bash
git add docs/plans/2026-06-05-v1-future-proof-rearchitecture-design.md docs/STATUS.md
git commit -m "$(cat <<'EOF'
docs(plans): mark Phase D done — re-architecture complete at v1.0.0

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 4: 実装計画を tracked 化**

```bash
git add docs/plans/2026-06-06-v1-phase-d-v1.0.0-implementation.md
git commit -m "$(cat <<'EOF'
docs(plans): track Phase D impl plan as dated snapshot

Co-Authored-By: Claude Opus 4.7 <noreply@anthropic.com>
EOF
)"
```

- [ ] **Step 5: memory 更新**

`aegis-rearchitecture-direction.md` に Phase D 完了を追記（**再アーキ F→R→A→D 全完了・v1.0.0**。migration guide(v0.12.2→v1.0.0)＋README リフレッシュ＋安定契約=運用契約のみ(CLAUDE.md/STATUS スキーマ/profile/gate/emit.sh 出力)＋SemVer ポリシー。INTEGRATION/CHANGELOG は YAGNI で out）。進捗を「全完了」に。git 管理外。

- [ ] **Step 6: grill-code（push 前）**

実装（ドキュメント）を grill-code でレビュー。migration の正確性・stale 残り・契約節の妥当性を疑う。指摘あれば fix-forward。

- [ ] **Step 7: push 前の最終状態確認**

Run: `git log --oneline origin/main..HEAD; python3 scripts/check_framework_contract.py >/dev/null 2>&1; echo "contract=$?"`
Expected: Phase D コミット群＋contract=0。`docs/architecture-overview.pdf` は含めない。

- [ ] **Step 8: push（ユーザー確認の上で実行）**

実行直前にユーザーへ確認してから `git push origin main`。

- [ ] **Step 9: git tag v1.0.0（push 成功後・ユーザー確認の上）**

```bash
git tag v1.0.0
git push origin v1.0.0
```

Expected: `v1.0.0` tag が origin に反映。tag メッセージは軽量 tag で可（必要なら `-a -m "Aegis v1.0.0 — future-proof re-architecture complete"`）。

---

## Self-Review

**Spec coverage（設計書 §1-6 → タスク）:** §2 migration → Task 1。§3 README stale → Task 2 Step 1。§4 安定契約/SemVer → Task 2 Step 2。§5 version 1.0.0 → Task 3。§6 検証 → Task 3 Step 4・Task 4。bookkeeping/tag → Task 5。out 項目（INTEGRATION/CHANGELOG/LEARNINGS）は非タスク＝設計どおり。

**Placeholder scan:** 各 Edit に厳密 old/new、migration table は実機確認済みの旧→新、コマンドは期待出力付き。§7 状態行・§11・Language Policy 位置は「着手前 grep 確認」を明記。プレースホルダなし。

**Type/identifier consistency:** skill 旧→新（brainstorming/review/security-review → aegis-brainstorm/aegis-review-gate/aegis-security-gate）を Task 1 table と Task 4 突合で一致。version old `0.12.6` → new `1.0.0` を Task 3 の3ファイルで一貫。tag は `v1.0.0`。安定契約要素（CLAUDE.md/STATUS スキーマ/profile/gate/emit.sh）を §4 と Task 4 Step 3 で一致。

**TDD 非適用の明示:** Phase D は新規コードなし＝検証ベース（contract/drift/全テスト緑維持＋tier eval＋migration grep 突合＋リンク健全性）。これは欠落でなく docs タスクの性質。
