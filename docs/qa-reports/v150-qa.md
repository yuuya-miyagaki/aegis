# v1.5.0 E1 activity verification — QA エビデンス（2026-06-11）

## TDD 来歴

- 全 13 タスク＋grill-code 修正 4 件を RED→GREEN→ミラー同期→コミットで完走（計画 docs/plans/2026-06-10-e1-activity-verification-implementation-plan.md）
- テスト件数: 389（v1.4.0）→ 425（Task 13 時点）→ **436**（grill 修正後）
- 代表的な RED 実証:
  - Task 5: post-bash-observe.sh が REQUIRED_HOOK_FILES 外で coverage FAIL → 配線で GREEN
  - Task 6+7: `current_fingerprint` 未定義 error 15 件＋test-result.json 書込 failure 1 件 → 置換実装で GREEN（中間赤なしの単一作業単位）
  - Task 8: evidence-log 不在で rc 0 ≠ 2 → 差し戻し実装で GREEN
  - grill 🔴-1: 非ASCII名・境界衝突・unreadable・quoted 名・削除検知の 5 件 RED → fingerprint 強化で GREEN
  - grill 🟡-2: template==example 配線パリティ RED（post-bash-observe.sh 欠落を検出）→ 登録で GREEN

## 検証コマンドと結果（最終、grill 修正後）

| 検証 | コマンド | 結果 |
|------|---------|------|
| 全ユニットテスト | `python3 -m unittest discover -s tests` | **436 tests OK**（v1.4.0 比 +47） |
| contract | `python3 scripts/check_framework_contract.py` | PASS: aegis contract is aligned |
| 参照 drift / mirror | `python3 scripts/check_reference_drift.py` | PASS: no reference drift detected |
| tier2 scaffold smoke | `python3 scripts/eval_scaffold_smoke.py` | PASS（minimal/standard/full） |

## 実地検証（抜粋）

- 観測→判定 e2e: post-bash-observe.sh 実発火（cwd≠記録 root）→ `read_test_result()=="green"`、post-bash.sh 失敗発火→ `"red"`、観測後のコード変更→ `"unverified"`（test_evidence_hooks.py）
- install 先実発火（smoke）: observer が `"cmd":"echo smoke-e1"` を追記、失敗側が `"status":"fail"` を追記（standard/full、F6 教訓の実発火検証）
- 判定マトリクス: ok/fail/なし × fp 一致/stale/oversize(nolib)/破損行/ローテーション `.1`/manual src/newest-stale 優先の 11 ケース（test_judge_card.py）
- fingerprint 決定性: クリーンツリー間の HEAD sha 差・非ASCII名の内容変更・境界衝突・docs/.claude 除外・oversize 境界・削除ファイル検知（test_fingerprint_lib.py 15 件）
- setup.sh gitignore 冪等性: 2 回実行後も evidence-log 行は重複なし（手動 smoke 実証）
- 生存チェック: evidence-log 不在で TaskCompleted 差し戻し（rc 2）・空ファイルは pass（liveness であって activity ではない）
