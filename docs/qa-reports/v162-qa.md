# v1.6.2 QA エビデンス（2026-06-13）

## 検証結果

| 検査 | 結果 |
| --- | --- |
| `python3 -m unittest discover tests -p "test_*.py"` | **Ran 683 tests in 147s, OK (skipped=1)**（v1.6.1 ベース 606 → 新規 77 件追加、skipped=1 shellcheck 未インストール） |
| `python3 scripts/check_framework_contract.py --profile=minimal --strict` | PASS |
| `python3 scripts/check_framework_contract.py --profile=standard --strict` | PASS |
| `python3 scripts/check_framework_contract.py --profile=full --strict` | PASS |
| `python3 scripts/check_reference_drift.py` | PASS（mirror byte-identical 確認） |
| `python3 scripts/eval_scaffold_smoke.py` | PASS |
| `python3 scripts/check_status.py --strict` | PASS |
| `bash tests/poc/v162-redteam-rerun.sh` | **18/18 passed**（REDTEAM-01, REDTEAM-01b, REDTEAM-02, REDTEAM-02b, REDTEAM-03, REDTEAM-04, F-01） |

## 新規テストの内訳

| ファイル | 件数 | 対象 |
| --- | --- | --- |
| `tests/test_test_marker_zero_run.py` | 12 | K-1 zero-run 3 軸判定 + 大規模 verbose 出力での head+tail 抽出 |
| `tests/test_control_plane_var_expansion.py` (追記) | 13 | K-2 cmdsub / 代替 assignment + grill Critical 1 の多段 redirect |
| `tests/test_secrets_quoted_var_and_cmdsub.py` | 16 | K-3 quoted var / K-4 cmdsub-built git + safe variant 抑止 |
| `tests/test_safety_lib_missing.py` | 5 | K-5 lib 欠落時の明示 deny |
| `tests/test_safety_fallback_identity.py` | 4 | K-5 fallback ブロック SHA256 一致契約 |
| `tests/test_hook_timeout_declared.py` | 3 | K-6 timeout 宣言契約 |
| `tests/test_safety_lib_registered_in_profiles.py` | 3 | K-5 + S-1 profile required 登録契約 |
| `tests/test_snapshot_atomic.py` | 2 | K-7 SIGKILL 中断下の atomic 化（20 iter） |
| `tests/test_snapshot_consumer_policy.py` | 3 | K-7 consumer 側ポリシー（snapshot 不在 / 部分破損） |
| `tests/test_setup_distribution.py` | 9 | K-8 / K-9 / K-11 / DIST-12 |
| `tests/test_setup_prereq.py` | 2 | K-10 |
| `tests/test_profile_checker_parity.py` | 4 | K-12 ARTIFACT_TO_TEMPLATE 双方向 parity |

合計新規 **76 件**（既存 1 件は K-1 fixture 修正で実質テスト方針変更）。

## 既知の flaky

`tests/test_failure_policy.py::TestPython3AbsentBehavior::test_python3_absent_advisory_hooks_do_not_crash` が **pytest フル走（test 順序依存）でのみ失敗** する。v1.6.1 オリジン状態でも同条件で失敗することを `git stash` ＋ 再走で確認済み（pre-existing flake、v1.6.2 で持ち込まれたものではない）。単独走（`python3 -m pytest -k test_python3_absent_advisory_hooks_do_not_crash`）と unittest discover では PASS。v1.6.3 / v1.7 で根本対処（おそらく framework repo 自身の `.claude/.gate-snapshot` を共有してしまう順序依存問題）。

## 反ガミング（marker_verified）

- 本リリース qa は **unittest discover** で OK 683 を確認
- record-test-result.py によるテスト緑色は judge カードで marker_verified を要求。本リリースでは pytest 系の verbose 出力が >4KiB の場合でも K-1 修正（head+tail 抽出）により `marker_verified=true` で記録される

## 結論

🟡 マージ可（ack で承認）。pre-existing flake 1 件は v1.7 で別途対処。
