# v1.5.1 grill 残余修正バッチ — QA エビデンス（2026-06-11）

## TDD 来歴

- Task 1〜7＋grill-code 修正 1 件を RED→GREEN→ミラー同期→コミットで完走（計画 docs/plans/2026-06-11-grill-residual-fixes-implementation-plan.md）
- テスト件数: 436（v1.5.0）→ 439（Task 2）→ 448（Task 5）→ **461**（Task 6 以降。grill 修正は既存テストへの fixture 追加のため件数不変）
- 代表的な RED 実証:
  - Task 1: parity fixtures 全面差し替え（`echo pytest` を True→False に反転、wrapper 形追加）で RED 5 件 → コマンド位置アンカー `_AEGIS_TR_PRE` で GREEN
  - Task 2: `test_multiline_command_classified` RED ＋ hook 実発火テストで **extract_command の grep 経路が JSON `\n` エスケープをリテラル 2 文字で返す**計画前提の誤りを検出 → 消費者 2 箇所の正規化（tr / .replace）＋ extract-input.sh fidelity ルーティング（計画逸脱として記録・grill で正当性確認）で GREEN
  - Task 3: TestDeployGateStderrSeparation 4 件 RED（stub check_status.py の stderr が deny 文面に混入）→ mktemp 分離で GREEN
  - Task 4: ロック順序の構造テスト RED（`mkdir "$LOCK_DIR"` の出現位置 > `CURRENT=$(`）→ ロック前倒しで GREEN
  - Task 5: dead-pid 回収 3 件 RED → pid ファイル＋atomic-mv claim プロトコルで GREEN（garbage/live pid 不回収も同時固定）
  - Task 6: TestControlPlaneWriteIndicators 9 件 RED（`grep "confirm " hooks/x.sh` false-deny ＋ `find hooks/ -exec dd of={} +` 素通り）→ 左境界＋find アクションフラグ封鎖で GREEN
  - grill A 🟡-1: `grep -E "(pytest|unittest)" log.txt` fixture RED（両エンジンで True）→ `(` のコマンド位置限定で GREEN

## 検証コマンドと結果（最終、grill 修正後 = 4c52528 時点）

| 検証 | コマンド | 結果 |
|------|---------|------|
| 全ユニットテスト | `python3 -m unittest discover -s tests` | **461 tests OK**（v1.5.0 比 +25） |
| contract（本体） | `python3 scripts/check_framework_contract.py` | PASS: aegis contract is aligned |
| contract（example） | `--profile standard --root examples/minimal-project` | PASS: project contract is aligned |
| 参照 drift / mirror | `python3 scripts/check_reference_drift.py --root .` | PASS: no reference drift detected |
| tier2 scaffold smoke | `python3 scripts/eval_scaffold_smoke.py` | PASS（minimal/standard/full） |
| STATUS 厳格検証 | `python3 scripts/check_status.py --root . --strict` | PASS |

## 実地検証（抜粋）

- T1 正味挙動（grill 両レビュー実測）: `grep vitest package.json`／`echo pytest`／`grep -E "(pytest|unittest)" log.txt` 不一致、`cd app && vitest`・`CI=1 npx vitest`・`(pytest)`・`uv run pytest` 一致。judge e2e（引数言及 fail が直前の実 green を覆さない）あり
- 改行正規化パリティ: `tr '\n' ';'` と `.replace("\n", ";")` は `\n` についてバイト等価。CRLF は境界クラスが `\r` を吸収。escaped-quote＋改行入りコマンドの観測→判定 e2e で実改行記録→green 分類を実証
- T2: deny/ask 文面の stderr 排除・空 stdout 時の診断併合・mktemp 失敗時 `/dev/null` フォールバック（`rm /dev/null` 回避ガード）・ERR_FILE リーク経路なし
- T3/T4 競合安全性: 2 contender×dead-pid stale lock の 15 回レースで torn write ゼロ・ロック残留ゼロ・常に単独勝者（grill A 実証）。取得前 exit 経路にリークなし、trap は pid→rmdir 順
- T5: 正当 JSON ペイロードで `find -exec dd`／`-exec sh -c`／`-execdir`／`-fprint(f)`／`-fls`／`-ok`／`"-delete"` 全 deny、`grep "confirm "`／`cat hooks/pre-exec.log`／`grep "truncate -s"` allow を実機確認
- ミラー同一性: 変更 7 ファイル（patterns.sh / extract-input.sh / post-bash.sh / check-deploy-gate.sh / check-control-plane.sh / build-judge-card.py / update-gate.sh）全て cmp で IDENTICAL
