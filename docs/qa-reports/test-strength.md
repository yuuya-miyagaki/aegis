# テスト強度ドリル結果（機械ブロック・ハーネス生成）

```
verdict: SKIP
reason: iter78（pytest execution attestation）は framework 改修を per-task commit 済み（dd06cca..HEAD）＝qa 承認時の working-tree diff（git diff HEAD）が空になる想定どおりの縁ケース（qa-verification SKILL 147-150）。`since:a5ef438` 案を実走し DRILL BLOCKED (anti-gaming) を実測（exit1）: coverage floor が (a) 新規/変更テストファイル4本〔test_attest_execution.py 1-608・test_judge_card.py 複数ハンク・test_record_test_result.py・test_test_runner_realness.py〕＝テストコード自体は変異で強度証明できない循環・(b) templates/profiles/full.json:41-42〔contract 随伴の config・非コード〕・(c) record-test-result.py:137-154〔redirect ハンク＝pin は test_record_rejects_pytest_family が実行不能を副作用で確認する形で捕捉済みだが floor は行ハンク単位で mutant を要求〕に mutant を要求＝framework 混在 diff の構造的不成立（LEARNINGS line33/54・iter76/77 と同型）。代替実証（手動 mutation 同等・全て review フェーズで親 in-session コピー側 or 本体 tree で実走・変異は必ず原状復帰）: (1) RED-first TDD＝Task1 の 28 pin を旧実装（実装前 a5ef438）で実走し 14 failed/14 passed を実測（全 RED は attestor 未存在/judge 旧契約による allow を assert 失敗で確認・実装で緑化）。(2) production 判断点 mutation バッテリ 7 種を親 in-session 実走し検知者を確立（変異注入→対象 pin だけ実行→RED 確認→即原状復帰→復帰後緑）: [M1 judge green制限 continue→pass]→test_observed/manual_pytest_ok_no_longer_decides が RED（2件）・[M2 src allowlist attested 除去]→test_attested_decides_* RED（4件）・[M3 attestor sessionfinish!=rc 突合削除]→**検知者不在を摘発**→test_forged_sessionfinish_mismatch_rejected pin 追加で封鎖・変異再走で RED を実証・[M4 rc==0∧executed==0 削除]→test_zero_run_* RED（3件）・[M5 executed から xfailed+xpassed 除外]→test_all_xfail_green RED（SF-015 解消 pin）・[M6 judge attested counts 検証削除]→test_handwritten_attested_*_fails_closed RED（2件）・[record redirect if fam: 削除]→test_record_rejects_pytest_family RED＝差分歯 7/7 に検知者確立（M3 gap は摘発→pin 追加で封鎖済み）。(3) 敵対クラス実走 7 種を親 in-session（tmp repo・本体 tree 不接触）: 出力偽造〔999 failed print×実 pass→green のまま＝出力非パース〕/exit 洗浄〔pytest;true→rc2〕/plugin 抑止 cmd・env〔rc2 or 明示 -p 勝ち〕/**event 偽造 5b〔all-skip＋偽 call passed→attested green 捏造＝残余 SF-024〕**/event 偽造 5a5c〔実失敗 suite に偽注入→real exit 勝ち red 記録＝green 化不能〕/attested 手書き〔fp のみ moat・既存 manual 天井同クラス〕。**load-bearing 不変「本物の red は偽イベントで green 化不能」を実走確認**・残余2は OS-limit（SF-024・drill subsume＝all-skip は marker false で BLOCKED を実測）。(4) 独立2次（盲検 agent ハード stall→親回収）: record↔judge drift 0〔同一 regex 共有〕・counts 壊れ耐性〔文字列/null/executed=文字列 全 unverified〕・log rotation 正常・plugin 例外妨害→red 記録。(5) full suite 1447 passed/2 skipped・ドッグフード attest green〔src=attested・executed=1447・2026-07-28〕・check_framework_contract PASS・check_reference_drift PASS・status_doctor PASS・context_budget PASS。詳細=docs/qa-reports/iter78-qa.md・iter78-review.md（対照表／角度別 mutation・敵対バッテリ）。
```
